require 'rails_helper'
=begin
RSpec.describe "RadioShows", :type => :request do
  describe "GET /radio_shows" do
    it "works! (now write some real specs)" do
      get radio_shows_path
      expect(response).to have_http_status(200)
    end
  end
end
=end